<!--Search Bar-->
<%= render 'form'  %>

<% @client.search("##{params[:q]} -rt", result_type: "recent").take(1).collect do |tweet| %>

<!---Tweet Content--->
<div class="row">
  <div class="col s6">
    <div class="center promo promo-example">
      <i class="fa fa-twitter" aria-hidden="true"></i><a href="https://twitter.com/<%= "#{tweet.user.screen_name}" %>" target="_blank"><strong> <%= "@#{tweet.user.screen_name}:" %></strong></a>
        <p>
          <%= tweet.text %>
        </p>
          <% tweet.media.each do | media |  %>
        <p>
          <%= image_tag(media.media_url_https.to_s, height: '163', width: '197') %>
        </p>
        <% end %>
    </div>
  </div>
<% end %>
    <div class="s6">
      <div class="center promo promo-example">
        <audio controls autoplay>
          <source src="<%=@tracks.first.preview_url if @tracks.first %>" type="audio/mpeg">
        </audio>
      </div>
    </div>

  </div>

<div class="container">
  <iframe src="<%= @picture.first.embed_url.to_s %>" width="" height=""></iframe>
</div>





<!-- <div class="container">


 <audio controls autoplay>
   <source src="<%=@tracks.first.preview_url if @tracks.first %>" type="audio/mpeg">
 </audio>

</div> -->
